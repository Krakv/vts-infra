apiVersion: v1
kind: Service
metadata:
  name: task-manager
  namespace: vts-dev
  labels:
    project: vts-app
spec:
  type: ClusterIP
  selector:
    app: task-manager
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
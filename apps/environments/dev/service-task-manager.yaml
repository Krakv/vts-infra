apiVersion: v1
kind: Service
metadata:
  name: task-manager-service
  namespace: vts-dev
  labels:
    project: vts-app
spec:
  type: NodePort
  selector:
    app: task-manager
  ports:
    - protocol: TCP
      port: 3006
      targetPort: 8080
      nodePort: 30006